doctype html
html(lang='en')
  head
    meta(charset='UTF-8')
    title Hello Pug!

  body
    h1 Pug — node template engine

  //- This is invisible comment
  #container
    - settings = { foo: 'bar'}
    include partials/amazing.pug
    // This is visible comment
    p(foo='agreement'
      data-foo)
      | Pug is a terse and simple templating language with a
      | strong focus
      | on performance and powerful features.
